import React from 'react';
import './Card.css';

interface CardProps {
  word: string,
  translation?: string,
}

function Card(props: CardProps): React.JSX.Element {
  return (
    <div>

    </div>
  );
}

export default Card;
